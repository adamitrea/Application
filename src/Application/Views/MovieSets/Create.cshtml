@model Application_DbAccess.MovieSet

@{
    ViewData["Title"] = "Create";
}

<h2 id="title">Create</h2>

<form asp-action="Create">
    <div class="form-horizontal">
        <h4 id="title">List</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="SetName" class="col-md-2 control-label" id="title">Name</label>
            <div class="col-md-10">
                <input asp-for="SetName" class="form-control" />
                <span asp-validation-for="SetName" class="text-danger" />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
